import { useEffect } from 'react';

export default function SmoothScroll() {
  useEffect(() => {
    // El snap scroll ahora está en CSS con scroll-snap
    return () => {};
  }, []);

  return null;
}
